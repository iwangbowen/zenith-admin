/** 告警等级标签色（告警页 / 仪表盘共用） */
export const IOT_ALARM_LEVEL_COLORS = { warning: 'orange', critical: 'red' } as const;

/** 物模型事件等级标签色（仪表盘 / 物模型抽屉共用） */
export const IOT_EVENT_LEVEL_COLORS = { info: 'blue', warn: 'orange', fault: 'red' } as const;