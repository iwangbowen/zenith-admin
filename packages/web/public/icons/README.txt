PWA / 桌面端图标目录
- icon-192.png (192x192 像素)
- icon-512.png (512x512 像素)
两者均为透明底、仅含 logo 本体，由 ../favicon.svg 生成：
  npm run icons -w @zenith/web
修改 favicon.svg（及同几何的 src/components/AppLogo.tsx）后请重新生成，不要手工编辑 PNG。
