{
  "version": "2.0.0",
  "tasks": [
    {
      "label": "DB: Migrate & Seed",
      "type": "shell",
      "command": "npm run db:migrate && npm run db:seed",
      "options": {
        "cwd": "${workspaceFolder}"
      },
      "presentation": {
        "reveal": "always",
        "panel": "shared"
      },
      "problemMatcher": []
    }
  ]
}
