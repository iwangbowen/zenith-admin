# 跨对象关联视图验收记录（2026-09-20）

## 已实施范围

- 统一实体引用、独立锚点解析器、领域关系提供者、权限与功能授权门控。
- 17 类锚点，按组懒加载、SQL keyset 分页、作用域绑定签名游标。
- 结构化操作审计、领域事件、通知、异步任务主体，事务性业务时间线。
- 支付退款、投诉、风控、对账审批闭环；用户、会员、流程、设备、内容、网盘、Wiki 接入。
- 人工关系建立/解除、双向查询、目标重新鉴权、界面和缓存复用。

## 自动化验证

- Shared：60 个文件、461 个测试通过，无类型错误。
- Server：全量执行 3044 个测试，其中 3025 通过、15 按配置跳过、4 个冷模块加载超时；4 个对应文件在单 worker、60 秒测试/Hook 预算下复跑，31 个测试全部通过。未修改业务断言或仓库超时配置。
- Web：245 个文件、1563 个测试通过，包含真实 Semi 渲染、缓存身份隔离和分页。
- PostgreSQL：独立本机临时库应用完整迁移，6 个真实集成测试通过；权限、跨租户、双向边去重、隐藏目标、微秒时间线分页、事务回滚与幂等均覆盖。测试后清空 fixture 并删除该临时库。
- 本地开发库迁移成功；全量 lint 通过，仓库既有 warning 保留。
- 管理后台、会员端、审批端的构建产物启动与登录链路冒烟通过。
- 产物预算复核通过，未放宽预算：总 JS 1008/1010，主入口 815/815，登录首屏 59/60 文件、996.0/1000 KB gzip。完整搜索/关联特性移出壳层静态依赖后，重新通过构建和四项启动冒烟。
- 最终分包调整后，首次 Ctrl/Cmd+K、Esc、快捷键偏好、查询缓存与真实组件渲染的定向回归通过；后端目录和时间线的 42 项相关回归再次通过。

## 实际浏览器

在用户打开的 `http://localhost:5373` 上使用真实后端，以管理员身份验证：

1. 支付订单详情关联组可打开，第一屏不查询所有分组。
2. 退款先显示 5 条，继续加载后显示完整 8 条，无重复，分页按钮正确消失。
3. 时间线先显示 20 条，再加载剩余 3 条；包含支付、退款、风控、投诉、通知、任务安全摘要。
4. 复用顶部统一搜索选择测试用户，建立人工关联并即时刷新。
5. 从订单打开目标用户，反向可见同一关联；从用户侧解除后双方刷新。
6. 建立和解除动作可由订单的操作审计分组按结构化主体反查。
7. 顶部搜索结果的“关联信息”按钮正确关闭搜索弹窗并打开关联抽屉。
8. 390 × 844 视口下检查抽屉、长标题、分组、反向关联与解除操作，随后恢复默认视口。
9. 分包优化后重新加载页面，复测首次 Ctrl+K、Esc 与订单详情退款关联列表。

浏览器首次接入曾因 Codex 的 apikey 认证不支持而失败；用户打开浏览器后连接恢复，上述交互验证已实际完成。一次开发热更新期间的时间线请求失败，经界面重试返回 200 并成功验证两页数据。

## 验证中修复的问题

- 关联类型与搜索类型语义分离，数据范围缺失归属列时不再静默放行。
- 改正全量读取后内存分页，锚点租户绑定和微秒级时间游标。
- 取消 Provider 内对所有异常转 404；仅授权后单组查询超时可局部降级。
- 修复分组命名冲突、契约操作 query key 冲突、用户/租户/模拟登录缓存隔离。
- 审计主体写入改为父子事务，签名审计测试替身更新后继续守住图片不入日志的断言。
- 修复两处误用 `List.Item.Meta`，采用 Semi 实际 API；补真组件渲染测试。
- 修复重复 React key、重复空态、搜索结果行内 Portal 冒泡与精确 ID 详情加载。
- 投诉退款的关联与事件进入退款持久化事务，事务失败不会调用支付渠道。

## 保留的浏览器测试数据

用户已授权创建测试数据。以下仅为本地 `zenith_admin` 的 `QA-关联视图` fixture，便于继续查看：

| 对象 | 标识 |
| --- | --- |
| 支付订单 | `payment.order / 1`，`QA-REL-1789877207625` |
| 退款 | ID 1–8 |
| 投诉 / 风控命中 / 风控审核 | 各 ID 1 |
| 异步任务 | ID 9，已完成 |
| 通知 | ID 12，终态、无收件人 |
| 人工关联测试用户 | `identity.user / 3`，已停用 |

支付应用、渠道配置和账户均为停用的沙箱依赖。没有实际付款或发送通知。测试所建的人工关联已通过界面解除，相关审计记录保留。

## 第二轮：来源反查与支付财务关联

本轮锚点从 17 类扩展到 25 类。新增通知、异步任务、操作审计的来源业务对象反查，以及订单/退款与凭证、对账案件、调整、分账、接收方、冲正、结算批次、已验证渠道回调之间的精确关联。复用已有数据表，不新增迁移或历史兼容逻辑。

### 自动化与真实数据库

- Shared 全量：60 个文件、461 项通过。
- Server 全量：3116 项中 3097 项通过、15 项按配置跳过，4 个既有冷模块加载超时；对应 4 个文件在单 worker、60 秒预算下复跑，31 项全部通过。没有放宽仓库配置或修改业务断言。
- 新增能力初次针对性验证 120 项通过；补齐结算自身凭证后，财务服务 57 项与 Mock 11 项通过。
- Web 全量：246 个文件、1574 项通过，含通知投递 ID 与 outbox ID 不同、无 outbox 隐藏入口、来源反查、财务作用域和结算双向查询。
- 本地真实 PostgreSQL 读取 63 个分组，以每页 2 条遍历并断言无重复：订单 13 笔凭证、结算 9 笔认领凭证及 8 笔退款、三个来源分组各 9 个对象均通过。已验签回调命中 1 条；同单号未验签回调反查订单为空；草稿调整的凭证为空。
- 全量 lint 无错误；既有 warning 保留。最终构建、产物预算和启动冒烟结果见下方最终验收。

### 浏览器实际检查

在用户已登录的本地页面使用真实后端验证：

1. 任务 ID 9 精确深链打开详情，任务列表“关联信息”能反查来源；5 条继续加载至 9 条。
2. 通知投递 ID 37 正确使用 outbox ID 12；来源从 5 条加载至 9 条，并能直达对应订单详情。
3. 操作日志筛选本轮 QA 记录，审计 ID 5 的来源分页完整展示 9 个对象。
4. 订单凭证按 5 / 5 / 3 加载，共 13 条，无重复且末页隐藏加载按钮；摘要使用中文财务语义。
5. 凭证→结算批次→退款分组完成两页共 8 条；关联对象在同一关联抽屉内导航。
6. 订单→对账案件→草稿调整；草稿调整未入账时显示无关联凭证。
7. 订单→分账单展示接收方、冲正记录，打开冲正后可返回原分账单。
8. 订单渠道回调只展示已验证且处理成功的 ID 1，未验签 ID 2 未混入。从回调列表分别打开两者，ID 2 的订单关联为空，ID 1 能准确导航至订单详情。
9. 390 × 844 下打开来源退款对象，标题、返回、分组均无截断；检查后恢复原视口。

开发热更新期间两次关联请求暂时失败，界面重试后成功；冻结实现后完成上述分页与跳转。评审中修复了结算批次自身 `settlement.initiated/paid/failed` 凭证双向关联的缺口，继续约束批次号、租户、应用、渠道账户和币种。

新增本地 QA fixture：凭证 ID 1–13、对账案件/调整/分账接收方/分账单/分账冲正/结算批次各 ID 1、回调 ID 1–2、通知投递 ID 37、审计 ID 5；沿用订单 1、退款 1–8、任务 9、outbox 12。数据均为 `QA-关联视图` 场景，脚本未执行渠道付款或通知发送，临时脚本已移除。

### 最终验收

- 核心工作区完整 build 通过；最终修订后 Server 与 Web 再次构建通过。
- 产物预算检查通过，没有提高任何预算阈值。
- 管理后台登录入口、会员端、审批端，以及后台登录→仪表盘→用户管理四项启动冒烟全部通过。
- 本轮代码分阶段提交到 `master`，未创建分支。用户打开的浏览器保留在测试订单详情，QA 数据保留便于复查。
