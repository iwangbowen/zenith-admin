# CMS 分阶段实施记录

范围：按 `next-stage-research-2026-09-23.md` 实施；全部生成式编辑、自动翻译和语义检索相关功能已从方案剔除。人工语言变体、确定性质量规则和 PostgreSQL 全文检索保留。

用户要求：阶段提交，中间省略 test/build/浏览器验证，功能完成后统一验证。新模型无需历史数据兼容。

## 已提交

- `f674b06a4`：调研方案与证据，剔除相关智能功能。
- `ea64eb1c4`：内容核心基础、工作稿/不可变修订/批准事实、完整回滚、CAS、模型与素材版本、结构化字段/正文、基础查看器、数据库迁移 0010。包含后续发布/编辑协作的基础表和契约；完整交互和发布运行时在后续阶段接入。

迁移 0010 已在开发库执行，包含数据库不可变修订触发器。尚未执行最终 test、lint、build 或浏览器回归。

## 正在集成

- 发布单/部署代次：隔离关系投影与文件目录、CAS 激活与回滚、精确修订预览、公共查询/搜索/静态一致、运行时撤下/素材撤权、固定事件快照、CDN outbox。
- 编辑工作台：保存保护、完整审核、字段渲染、独立内容模型、任务视图和发布操作。
- 编辑协作：批注及提及通知、质量报告、人工翻译组与源稿变化、内容生产指标。
- 内容复用：显式类型转换预览、跨站三方差异及人工选择、素材版权和版本管理。

## 最终验收入口

1. 完成全部入口与 Mock 的新契约接线，初始化新版 seed。
2. 生成并执行剩余迁移；检查迁移只涉及本次 CMS 改动。
3. 统一运行 test、lint、build，并修复发现的问题；前端构建需提供 `VITE_DEPLOYMENT_ID`。
4. 在 localhost:5373 实测工作稿与线上隔离、精确修订审批、全字段恢复、并发冲突、新建防丢稿、模型发布、素材固定版本、发布失败/激活/回滚、批注/翻译/分发合并。
5. 记录实际结果、验证边界和各阶段提交，不能将未经验证的代码描述成已经通过验收。
