# CMS 分阶段实施记录

范围：CMS 内容核心重构；不包含 AI 功能。人工语言变体、确定性质量规则和 PostgreSQL 全文检索保留。

用户要求：阶段提交，中间省略 test/build/浏览器验证，功能完成后统一验证。新模型无需历史数据兼容。

## 已提交

- `7df3a55d0`：内容核心基础、工作稿/不可变修订/批准事实、完整回滚、CAS、模型与素材版本、结构化字段/正文、基础查看器、数据库迁移 0010。
- `fd79d0389`：发布代次、内容治理后端、确定性检索与站群同步基础。
- `61175a546`：编辑工作台、配置冻结、发布治理和首轮联调修复。

迁移 0010–0012 已在开发库执行，新版 seed 已初始化；包括不可变修订和发布输入触发器。

## 第三阶段完成内容

- 发布单/部署代次：隔离关系投影与文件目录、CAS 激活与回滚、精确修订预览、公共查询/搜索/静态一致、运行时撤下/素材撤权、固定事件快照、CDN outbox。
- 编辑工作台：保存保护、完整审核、字段渲染、独立内容模型、任务视图和发布操作。
- 编辑协作：批注及提及通知、质量报告、人工翻译组与源稿变化、内容生产指标。
- 内容复用：显式类型转换预览、跨站三方差异及人工选择、素材版权和版本管理。
- 配置与素材依赖固定、发布日志、历史分页、前端与 Demo 新契约接线。

## 2026-09-23 统一验收

- Shared：最终全量 68 文件、487 测试通过，无类型错误；新增发布一致性规则专项通过。
- Web：最终全量 266 文件、1654 用例通过；最后冲突展示专项 3 用例通过。
- Server：最终完整运行 386 文件、3214 用例通过；4 文件、22 用例按既有条件跳过，无失败。健康检查、监控、调度器冷加载与假时钟隔离已修正，业务测试仍保持 15 秒时限。
- Shared、Analytics SDK、Server 和 Web 完整 build 通过；lint 全部通过（Server 9 条、Web 101 条已有警告）。Web 三入口、预压缩和 bundle 预算通过。
- 真实 API 完成 12 组业务回归，详见 [验收证据](./research/2026-09-23-final-api-results.json)。涵盖发布激活、工作稿隔离、陈旧/缺失 CAS、固定送审稿、完整恢复、批注/质量/人工翻译、代次回滚、紧急撤下、列表/日历、素材固定版本与撤权、模型固定版本、成组内容与页面配置冻结、回滚后的工作稿一致性。
- 浏览器验收覆盖内容列表、新建自动保存、刷新保留标题/摘要/正文、完整稿件/历史修订、批注处理、质量面板、真实并发 409 与中文业务字段三方对照、采用最新服务器稿、发布单详情与激活记录、固定候选手机预览、素材 v1/v2 与授权表单。390px 编辑页无横向溢出；候选手机预览宽度 375px，iframe 禁止脚本。
- 测试内容 `QA-CMS-UI-0923`（id 13）及 `QA-CMS-FINAL-*`、测试素材、模型和发布单保留在开发库。
- 已修复真实 PostgreSQL 暴露的列表子查询别名、原始 SQL 日期编码、候选部署发布时间查询问题；定时发布连续五次运行成功。
- 编辑页刷新基稿与 CAS 同步、防止本地修改被覆盖；历史预览和素材版本缓存失效；固定模型列显示；草稿保存不受发布必填限制；桌面长内容滚动问题均已修复。

按用户要求已合并 `master`，移除原调研提交及其文件；后续修改直接在主分支实施。未推送远端。

## 验证边界

开发环境验证使用真实 PostgreSQL、任务 worker、文件存储与浏览器；多角色复杂审批、外部 CDN/Webhook/搜索引擎真实投递及大站性能没有在本轮端到端验证。分发三方合并有共享算法和 Mock 回归。公开关系快照目前采用全量复制，运行成本和逐路径无脚本预览边界见 [发布运行说明](./publication-generations.md)。
